# Palindrom number program

num =121  #example number
temp = num
reverse = 0
while(num > 0):
    digit = num % 10    # extract digit from the number one by one from the right
    reverse = (reverse * 10) + digit # build the reverse number by shifting and adding digits
    num = num // 10 # remove last digit
    
if temp == reverse:
    print("Palindrom")
else:
    print("not a palindrom")
